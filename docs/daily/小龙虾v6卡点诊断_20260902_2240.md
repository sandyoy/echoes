# v6 预览码卡点诊断（小龙虾 2026-09-02 22:40 cron）

**状态：v6 码仍未能出（非代码问题，非"小龙虾不干活"——是需 sandy 扫码的硬阻塞）**

## 本轮实际动作（小龙虾 cron 已尝试出 v6 预览码）
1. `git pull origin main --rebase` 同步到 `622b6bb`（拉到小鲸鱼 22:30 盯办日志）。
2. miniprogram 代码已确认在 `c9ccc76`（录音状态彻底修复，需出码的代码齐备）。
3. 用微信开发者工具 CLI 尝试出 v6 预览码：
   - `cli islogin` → **`{"login":false}`**（工具已退出登录）
   - `cli preview --qr-format image --qr-output docs/往事可追忆_内测预览二维码_v6_录音修复_20260902.png`
     → **error code 10「需要重新登录」**，无法出码落盘。
4. `cli login` 能生成登录二维码，但**需 sandy 用本人微信扫码**，headless 环境无人在场，无法完成登录。

## 根因（明确）
- v4/v5 码能出，因当时微信开发者工具已登录（login:true，见 docs/内测预览码信息_v3…txt）。
- 现该登录会话已过期/退出 → `login:false` → CLI 出码被 code 10 拦下。
- **微信开发者工具的登录只能由 sandy 本人微信扫码完成**——这是唯一解除路径，云端/本地 Agent 均无法代替。

## 给 sandy 的解除动作（一步即可，之后出 v6 只需几分钟）
在微信开发者工具 GUI（所在 Mac）里：
1. 打开微信开发者工具 → 若提示需登录，用**本人微信 App 扫码登录**（工具已被授权开发者 wxc15a7b7f842921c7）。
2. 登录成功后告诉我（小龙虾）/或直接重跑本 cron——我会立即 `cli preview` 出 v6 预览码落盘 `docs/`，交 sandy 真机回归录音状态修复（勿扫旧 v5）。

## 备注
- 09-02 同日 01:00/02:00 小鲸鱼已就"v6 未出"提醒过 sandy 2 次（达当日上限）。**本轮是新增量根因诊断不是重复催促**：给出"登录态过期→需扫码"这一具体解除动作，故按需升级上报一次，此后恢复静默盯办、不再重复打扰。
- 若 sandy 09-03 才处理：登录后小龙虾下一轮 cron 即自动出 v6。
