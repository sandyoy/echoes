# Echoes 内测盯办 — 状态跃迁记录（0907 末 / v7 后端到①前置）

> 由盯催 cron 在本地发现并更正：原"卡=小龙虾出 preview 二维码"的判定已过时。

## 一、原盯催前提已解决（非卡点）
- 09-07 小龙虾已连续出 **v6**（19:36，`docs/往事可追忆_内测预览二维码_v6_录音修复_20260907.png`）与 **v6b**（22:25，`..._v6b_录音修复_20260907_2225.png`）录音修复版预览码。
- v6b 已于约 22:30 转交 sandy（TASK_BOARD 已留档、commit 619010c 关闭该转发协办）。
- 结论：**"出 preview 码" 这一目标本身已多次达成，非当前阻塞。**

## 二、项目已进入 v7 改造阶段，真正的球在后端（云端=本机）
- sandy 已确认 v7 方向（对齐 v6 真机 5 问，见 `docs/功能需求表_Echoes_V7改造_v20260907.md`）。
- 小龙虾已把 **前端 v7 全部落地并自测**（0e5fe23：统一保存页/可改字/回放原声/选年份归轴/录音一键结束防卡死/本地必存）。
- 小龙虾已钉协作牌（`coop_tasks/小龙虾@小鲸鱼_v7后端到①信号与出最终码唯一出口_20260908.md`，status=**待认领**，assignee=**小鲸鱼/后端**）：
  - **B（ASR 迁讯飞）到① = 最高优先级**，其后 A 出字/C 打断都靠它"嗓子";
  - E（story 年份字段+排序+旧数据兜底）;
  - C（采访会话续接+TTS 打断到①）;
  - /api 前缀核对（`app.globalData.apiBase=https://yuanaikang.cn/api`）。
- 小龙虾明示：**收到后端"到①"信号，才合测到③出最终 v7 码；sandy 只做⑤真人最后一次。**

## 三、后端现状核验（本机仓库）
- `backend/asr.py`/`server.js`：仍走百度(失效)→whisper 兜底，**无讯飞迁移痕迹** → 待做。
- story 年份：server.js 有部分 date 写入，非完整 models/story 年份字段+兜底迁移。
- 无未push本地改动。→ B/C/E 到① **尚未完成**。

## 四、本轮动作
- 已向 sandy 发状态跃迁更正：不用扫已可能过时的 v6b 当小白鼠；后端 B→① 是当前真正缺口，云端已把它列为优先执行项，完成后给小龙虾信号合测到③，再出最终 v7 码交 sandy ⑤ 一次。
- 本轮为"状态更正 + 主动兜执行"，非重复 nag。

— 小鲸鱼（云端·盯办 cron）2026-09-07
