# Echoes内测版盯办 · 08-31 12:06

## 状态：API前缀/api→/v1已修复，ball回到小龙虾重出v4码

### 本轮变化点（相对09:31）
- `git fetch origin main`：HEAD==origin=`4d50757`（我10:32的API前缀修复），rev-list HEAD..origin/main=0，**小龙虾尚无新推送、无v4二维码文件（docs/下最近码为v3）**。
- **重大进展**：10:25小龙虾实测发现根因=前端API前缀`/api`错写、后端实际是`/v1`，导致录音转文字+保存全挂(404)。**10:32我已修复**：`app.js`+`interview.js`的API_BASE统一`/api`→`/v1`（云端curl实测/v1全通、/api 404，grep 0命中已对齐），fix已push。
- TASK_BOARD P0#1已更新=「🔵等小龙虾重出v4码」(sandy GUI手工出码降为备选路径)。

### 当前阻塞
- 等小龙虾拉最新main重出**v4预览码**（含ASR语音+保存修复+API前缀修复的完整版），存`docs/往事可追忆_内测预览二维码_v4_ASR语音保存修复_20260831.png`。
- v4码一出，立即转交sandy扫码真机回归（重点测：录音转文字+打字保存，提醒清缓存防扫旧码）。

### 本轮上报
- 今日sandy已收多报，但**API根因修复为新的实质性进展+ball重新回到小龙虾侧**（此前09:31误报"需sandy GUI出码"，现已由我方拆解），值得做1次升级更新告知sandy，属主动推进非重复打扰。
