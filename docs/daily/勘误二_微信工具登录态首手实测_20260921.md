# 勘误二 · 微信开发者工具登录态·首手实测复核（小龙虾 2026-09-21 13:3x）

> **本卡用途**：把 09-20 我上报③真机回归阻塞时**引用的登录态措辞**做一次第一手纠正，并记录今日实测原始证据。
> **只读不改**：本轮未出码、未改任何代码/配置/服务、未碰对方文件。

---

## 一、前一条卡里的一处措辞不准（自我纠正）

`coop_tasks/小龙虾@小鲸鱼_③真机回归接单_后端已验证_环境阻塞上报_20260920.md` 第 53 行写：

> 「DevTools CLI 已装且 `islogin` 返回 `login:true`」

**该措辞不准确**。今日（09-21 13:3x）我在本机**第一手实测**：

```
$ /Applications/wechatwebdevtools.app/Contents/MacOS/cli islogin
✔ IDE server has started, listening on http://127.0.0.1:54181
{"login":false}
```

→ 真实状态是 **`login:false`（已掉线）**，与「login:true」相反。原文属笔误/记忆偏差。

**结论不变、判据更强**：当时及此刻，`preview` 均因登录失效而失败。此前「需重登」的判定**成立**，只是支撑措辞写反了。

---

## 二、今日首手实测原始证据（cron 环境实跑）

| 项 | 结果 |
|---|---|
| DevTools 是否安装 | ✅ `/Applications/wechatwebdevtools.app` 存在，CLI 可执行 |
| DevTools 是否在运行 | ✅ **进程正在跑**（nwjs Helper，起于 09-20 21:26；IDE server 监听 127.0.0.1:54181） |
| 登录态 `islogin` | ❌ **`{"login":false}`** |
| `preview` 出码 | ❌ `{ code: 10, message: '需要重新登录 (code 10)' }` |
| 本次产出码 | 无（未出码） |

原样报错（节选）：
```
[error] { code: 10, message: 'Error: 需要重新登录 (code 10)' }
  at .../package.nw/core.wxvpkg/3f831e7255c55b0464f2f1c29d9d043e.js ...
```

**根因判定（第一手，非转述）**：**卡点在「微信登录态失效」，不在 DevTools 未安装、也不在工具未启动。** 只要真人扫码重登一次，`preview` 即可出码。

---

## 三、对 09-24 硬节点的直接影响

- 出码**唯一拦路**＝一次**真人扫码重登**；工具与工程侧**零阻塞**（工具在跑、CLI 可用、代码已推）。
- **建议动作（仍需 sandy 拍板，我不自主执行）**：请 sandy 打开已运行的微信开发者工具**扫码重登** → 重登后我即可自动跑 `cli preview` 出码。
- 另：真机 9 条回归仍为**手感/交互验收**（重点第 4 条「AI 说话中按麦克风即停」），必须真机+人手，cron 无法代跑，**不代报已过**。

---

*小龙虾 2026-09-21 · 本卡只做勘误与证据留痕，不改任何代码/配置/服务，不碰对方文件*
