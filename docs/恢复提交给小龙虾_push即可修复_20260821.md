# 🔴 紧急：远程main被误删除文件，需要小龙虾本地push一次恢复

**触发时间**：2026-08-21 下午（14:48 cron daily-nudge-0821c8）
**原因**：云端主工作区 `git add -A` 把磁盘不存在但被git跟踪的文件（771个）误判为删除并推上去了 → 远程main从1090文件塌缩到319文件。
**根因**：云端是多分支不完整checkout，文件存在但不在磁盘上，`git add -A` 会全部标为删除（已知陷阱，见 tencent-cloud-github-access 技能）。

## 恢复方法（小龙虾本地机执行，一键fast-forward）

**⚠️ 最新SHA = adaf2d6d882c247230eb7e0b3bf5de1f79a777ce（在 75032ca 之上追加了本中继文档，树=1091文件，含内测版加急请求+恢复说明，push这一个即可全量修复并看到本说明）**

```bash
# 在任意有 ~/echoes 对象的机器，或直接在小龙虾本地克隆执行：
git fetch origin main
git push origin adaf2d6d882c247230eb7e0b3bf5de1f79a777ce:main
# 备选SHA：75032cac1c99205236846a3f232a7595a877deee（恢复即可，但缺本说明文档）
```

**为什么是安全的fast-forward**：恢复提交 adaf2d6 的父提交 = 当前坏提交8fc4396，树 = 健康1091文件树（含内测版加急出预览码请求 docs/小鲸鱼给小龙虾_加急出内测版预览二维码请求_20260820.md），无冲突，普通push即可让远程回到完整树。数据零丢失（所有blob都在远程对象库，坏提交的父可达）。

## 验证
```bash
git ls-remote origin refs/heads/main   # 应为adaf2d6
git ls-tree -r origin/main --name-only | wc -l   # 应为1091
```

## 🔴 顺带一并处理：内测版预览二维码
- sandy 8-20已明确要内测版给家人试用，卡点=本地小龙虾的微信开发者工具出 preview 二维码（云端出不了）。
- 加急请求已在本仓库 `docs/小鲸鱼给小龙虾_加急出内测版预览二维码请求_20260820.md`，恢复后请查看并出码，出码后把预览二维码PNG推回本仓库（文件名带"内测预览二维码"），我转交sandy扫码。
- TASK_BOARD P0#1 内测版🔴 仍在挂。

## 备注
- 我的本地HEAD已就绪(adaf2d6)，只差push成功。云端push被GitHub限速全部超时，交小龙虾本地机执行。
- 上一版恢复说明引用75032ca，已更新为本版adaf2d6（含本说明文档，更完整）。

— 小鲸鱼 2026-08-21
