# 今夜作业总账（2026-09-24 夜 → 09-25 早）

> **用途**：sandy 明早一问就能对上。**不靠记忆，靠这张表。**
> **规矩**：每完成一件，本表状态同步更新；推 GitHub 后打勾。
> **背景**：sandy 09-24 晚指出两件事——(1) 分工本就是我该干的，我拿"等拍板"当挡箭牌是错的；(2) 我说了要做的事没做，两人还都想在 sandy 那边要决策。本夜起改：**能自己干的，直接干完**。

---

## 一、今夜要干的活（7 件）

| # | 事项 | 归属 | 状态 | 产物 |
|---|---|---|---|---|
| 1 | **ASR 切讯飞**：`asr.py` 改「讯飞优先 → whisper 兜底」 | 小鲸鱼 | ✅ 代码完成 | `backend/asr.py` |
| 2 | **按 v2.4 重写数据模型**：故事为主体 + 素材筐 + 时间双维度 + 时间精度 + 待认领 + 两级权限 | 小鲸鱼 | ✅ **完成·实测通过** | `backend/models.py`（11 表） |
| 3 | **时间轴提取算法原型**：三类线索 + 时间待定 + 模糊原样保留 | 小鲸鱼 | ✅ **完成·实测通过** | `backend/timeline.py` |
| 4 | **心脏康复 6 个待核项** → 用 6 份文献换依据 | 小鲸鱼 | ✅ **完成** | 《6项待核项文献依据对照表》+《改动清单》 |
| 5 | **6 份文献归档** → `wiki/medical/文献资料/心脏康复专题/` | 小鲸鱼 | ✅ **完成** | 6 PDF + 提取文本 |
| 6 | **落盘《09-23~09-24 心脏康复对话决议》** + 讯飞凭据内部文档 | 小鲸鱼 | ✅ **完成** | `心脏康复项目/` + `docs/内部/` |
| 7 | **推送 GitHub + 自验 4 步** + 明早简报 | 小鲸鱼 | ⏳ 进行中 | commit + 简报 |

### 【第1件·ASR】实测结论（真实跑过，非静态检查）

| 验证项 | 结果 |
|---|---|
| 代码改造 | ✅ `asr.py` 已改为**讯飞优先 → 百度次选 → whisper 兜底**，语法通过 |
| 签名算法 | ✅ 真实 key 可生成鉴权 URL（长度 315） |
| 依赖 | ✅ `websocket-client 1.9.2` 已装（在 `/home/ubuntu/.local`）、`ffmpeg` 已装 |
| **真连讯飞** | ❌ **报错：`HMAC signature cannot be verified: apikey not found`** |
| 推断根因 | **`.env` 里的讯飞 `XF_API_KEY` 无效**（格式长度都对，但服务端查不到该 apikey）——**须重取** |

> **⚠️ 给 sandy 的待办**：讯飞 key 可能抄错或未开通「语音听写」服务。请在讯飞控制台核对：① 应用是否开通 **语音听写(IAT)** ② 三个值是否与「WebAPI」页签一致（注意不是"语音合成"那组）。**这是唯一卡住 ASR 真跑通的点。**
>
> **另**：whisper 兜底当前跑不通（本机 numpy 环境冲突 `No module named 'numpy._core._multiarray_umath'`），需单独修环境。**不影响讯飞优先路径**，但兜底暂时失效。



---

## 二、已核实的环境事实（今夜实测，非推测）

| 项 | 实测结果 |
|---|---|
| `.env` 讯飞三件套 | ✅ 全部有值：`XF_APP_ID` 8 位、`XF_API_KEY` 24 位、`XF_APISECRET` 32 位（格式合规） |
| `.env` 是否泄漏 | ✅ `.gitignore:16 backend/.env` 已忽略，不会进公开仓 |
| `asr.py` 现状 | ❌ 引擎顺序仍是**百度优先 → whisper 兜底**（最后改动 08-30），**讯飞未接入** |
| `models.py` 现状 | ❌ 仍是**旧四表**：`User / Story / AudioClip / Event / Photo`（最后改动 08-19），v2.4 新模型**一张未建** |
| Python 依赖 | ✅ `requests`、`faster_whisper` 已装；❌ `opencc`（繁转简）未装，需补或改用手写映射 |
| 分工依据 | v2.4 第十三节：**小鲸鱼 = 后端 / ASR / 需求落地 / 时间轴算法** |
| 我名下待验证项 | v2.4 待验证清单 #4 时间轴、#8 ASR选型、#9 权限表结构、#11 时间vs故事表结构、#13 同一件事语义判断、#16 待认领机制 |

---

## 三、我明确"不动"的东西（sandy 已交出去或已审）

| 项 | 原因 |
|---|---|
| ❌ 14 个动作（动作库） | sandy 已交人做图，一个字不碰 |
| ❌ 5 个状态式套餐结构 | 已定版 |
| ❌ 套餐价格表 v4.1 横版 | 已审 |
| ❌ 郑大二附院勾选表 v1.0 | 已审 |
| ❌ 《运动疗法对照表_只给医生》v1.0 | 已审，不重发 |

**心脏康复这条线我只做一件事**：把 v1.1 待审稿里的 **6 个「待核」**（抗阻起始负荷 / PCI 启动时机 / FITT 分阶段 / 八段锦呼吸配合 / 6 分钟步行分级 / 绝对禁忌症）用 6 份文献逐条换成"依据+出处"。**改的是依据栏，不是动作本身。**

---

## 四、明早 sanday 可能问的问题 → 我直接答

| 你可能会问 | 答案位置 |
|---|---|
| Echoes 后端做到哪了？ | 本表 §一 #1/#2/#3 |
| ASR 用了讯飞没有？ | 本表 §一 #1 |
| 心脏康复那 6 项填完了吗？ | 本表 §一 #4 |
| 文献归档没？ | 本表 §一 #5 |
| 你昨晚说的决议文件呢？ | 本表 §一 #6 |
| 推仓库了没有？ | 本表 §一 #7 + 推送自验 4 步记录 |

---

**最后更新**：2026-09-24 深夜
**维护人**：小鲸鱼（云端）
