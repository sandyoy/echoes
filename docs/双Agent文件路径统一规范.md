# 📁 双Agent文件路径统一规范

> 维护人：小鲸鱼 + 小龙虾
> 最后更新：2026-07-27
> 规则：所有人按此规范存放文件，谁违反谁改

## 根目录

`~/echoes/` — 唯一工作根目录。

所有协作文件、代码、知识库、方案文档，**全部**放这里。不允许在 `~/echoes/` 之外另建工作目录。

## 目录结构

```
~/echoes/
├── TASK_BOARD.md          # 全局任务板（唯一的同步枢纽）
├── miniprogram/           # 微信小程序代码
├── web/                   # 前端网站代码
├── backend/               # 后端代码
│
├── docs/                  # 所有方案、报告、分析文档
│   ├── daily/             # 每日讨论记录（存档用）
│   ├── 圆爱康/            # 圆爱康业务方案、合同、产出
│   │   ├── 01_项目方案/
│   │   ├── 02_合同/
│   │   ├── 03_产出方案/
│   │   └── 04_PPT/
│   └── *.md               # 单篇文档
│
└── wiki/                  # 知识库
    └── medical/           # 医疗知识库
        ├── 圆爱康产品资料/  # ← 小龙虾把47个zip的内容放到这里
        ├── 招标采购文件/    # 已有
        └── 临床指南/       # 后续补充
```

## 关键规则

| 规则 | 说明 |
|------|------|
| **唯一根目录** | `~/echoes/`，不允许其他地方 |
| **圆爱康知识** | 全部在 `~/echoes/wiki/medical/圆爱康产品资料/` 下 |
| **方案/合同/PPT** | 全部在 `~/echoes/docs/圆爱康/` 下 |
| **Git同步** | 谁更新谁 `git push`，对方 `git pull` |
| **历史文件迁移** | 小龙虾把 `~/yuanekang/` 和 `~/HermesFiles/` 下的内容整理到上述目录后，旧目录可删除 |

## 立刻要做的事（小龙虾）

1. 把 `~/yuanekang/inbox/` 下的47个zip解压后的内容，整理到 `~/echoes/wiki/medical/圆爱康产品资料/`
2. `git add . && git commit -m "📚 迁移圆爱康产品资料到统一知识库" && git push`
3. 我（小鲸鱼）`git pull` 后就能看到了
