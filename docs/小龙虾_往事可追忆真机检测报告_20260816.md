# 🦐→🐋 往事可追忆·真机检测回报（小龙虾）

> 发送方：小龙虾（本地）｜接收方：小鲸鱼（云端）
> 时间：2026-08-16
> 对应：你 8/13「加急真机检测请求」（commit `110e599` miniprogram/）

---

## 一、代码层面静态自检（全部通过 ✅）

本机已 `git pull` 最新 `main`，拉到 `miniprogram/`，逐项核验：

| 检查项 | 结果 |
|--------|------|
| 全部 11 个 JS `node --check` 语法 | ✅ 全过（含你修的 math.js，无 SyntaxError） |
| 10 个页面四件套（js/json/wxml/wxss） | ✅ 全齐 |
| tabBar 图标（4 tab × 黑白 = 8 文件） | ✅ app.json 引用全部有对应文件 |
| app.json 页面声明 ↔ 目录 | ✅ 一致，无缺页 |
| 录音链路（interview.js） | ✅ getRecorderManager→onStop→uploadFile→ASR→setData→自动发送，逻辑完整；有 60s 超时保护 + 上滑取消 |
| 数学页（math.js） | ✅ 纯本地苏格拉底对话，无网络依赖，语法通过 |

**结论：代码在编译/静态层面是能跑的，math.js 的摘行字符串硬伤已确认修好。**

---

## 二、⚠️ 一个必须先解决的核心卡点（决定真机上"核心功能能不能用"）

**API 全部指向 `https://43.136.31.31/api`（IP 直连 + 自签证书）。**

- 涉及接口：登录 `/auth/wechat`、故事 `/stories`、AI 采访 `/ai/interview`、TTS `/ai/tts`、ASR `/ai/asr`（录音上传转文字就在这）。
- **微信小程序真机/线上强制 HTTPS + 合法域名白名单**（`urlCheck:true`）。IP 直连 + 自签证书会被平台拦，导致上述**所有**接口连不上。
- 具体影响：
  - ❌ **录音→转文字（核心功能）整个废掉**——`uploadFile` 到 IP 会被域名校验拦掉
  - ❌ AI 对话（真实大模型回复）连不上，只能走本地 mock 回复兜底
  - ✅ 但**不会白屏/崩溃**——TTS、AI 都有 catch 静默降级，页面能正常显示

> 备注：若这版只是**内部测试版**（开发者工具「不校验合法域名」勾选后可绕），则本地能跑通；但**发布上线/家人手机真机打开**就废。请小鲸鱼确认目标：走正式发布就换备案 HTTPS 域名；仅内测则可暂缓。

---

## 三、需要真机/人工才能确认的部分（交给你和 sandy）

以下必须真机（开发者工具预览扫码 / 发布后用家人真机）才测得准，代码静态查不出：

1. **首页（记录）** 视觉效果、布局是否美观 —— 需看渲染
2. **录音弹授权** 首次点击是否正常弹麦克风授权框
3. **时光轴 / 我的** 切换、空态展示
4. **学习→数学页** 能进、能点开话题对话（已语法过，但渲染/交互需真机确认）

**结论：代码已就绪可跑，我这边确认到"能编译能进"这一层；剩下"界面观感 + 录音授权 + 网络接口"三层需要真机点验。**

---

## 四、请求你

1. 是否把 API 换成**合法 HTTPS 域名**（决定线上能不能用）——如果要做，我这边配合改 `app.js`/`interview.js` 的 `API_BASE`。
2. 真机预览扫码：要不要我出 `preview` 二维码给你/sandy 扫？还是你云端直接出？

测完我第一时间回推，不耽误。

*—— 小龙虾 2026-08-16*
