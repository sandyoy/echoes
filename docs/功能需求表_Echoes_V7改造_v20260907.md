# Echoes · v7 改造 功能需求表（开发依据 / 测试依据 / 活联动图）

> 制度依据：sandy 功能需求表铁律（2026-09-03）——无表不算开工；任何改动必带"依赖/联动"列，改一条要同步回归被牵动的条。
> 范围：对 v6 真机 5 大问题的改造（对齐 a1adbe5 小龙虾《内测改造设计定稿 v7》）。owner：小鲸鱼(云端·一人写到底)，小龙虾负责出码+真机回归。
> 决策：**自测到③(真机)才算过，仅⑤(真人体感)才找 sandy**。读表人=任一技术人员可照做。
> 五层：①云端curl/脚本打接口 ②云端页面/逻辑自测 ③真机回归(小龙虾) ④真机返数云端复验 ⑤真人(sandy/老人)

## 场景 A — 自述"按住说话"出可编辑文字确认框（sandy 最强调）
| 现状 | 改造后 | 验收(步骤化·能照做) | 由谁测/层 | 状态 | 依赖/联动(改它牵动谁) |
|---|---|---|---|---|---|
| 松手即自动存，无文字确认，ASR败降级[语音回忆X秒] | 松手→弹可编辑文字框(实时显示识别文字)→【改一改再存】【直接保存】→存成 type=audio：正文=文字content+可播audioPath | 按住说一句→弹框出现文字→能改错字→点任一钮→提示成功；时间轴出现该条带文字 | ①(接口存拉)+③ | 待改 | **牵动最广**：A3(ASR出字)、A4编辑保存、A5时间轴、共享录音单例app.js(采访同受影响)、story详情播放 |

## 场景 B — 语音转文字底层 ASR 真通（根子嗓子，先于此前提）
| 现状 | 改造后 | 验收 | 由谁测/层 | 状态 | 依赖/联动 |
|---|---|---|---|---|---|
| asr.py 百度(失效)→whisper 兜底；方言/口音+长句识别差 | 迁讯飞(老人方言/抗噪强，sandy 8-30 已给key)。先定位 /api/ai/asr 路由当前由谁承载（backend/main.py 未见该路由→疑 node 服务或前缀不符，需先核清再迁） | curl 传一小段老人话 mp3→返同义中文文本、非"(未能识别)"占位 | ① | ⚠️待定位后迁移 | 所有"出文字"环节咽喉：A1出字、场景C采访语音→字、mine计数、时间轴正文 |

## 场景 C — AI采访"可打断一问一答"（不尬等整段回）
| 现状 | 改造后 | 验收 | 由谁测/层 | 状态 | 依赖/联动 |
|---|---|---|---|---|---|
| 录音→整段发 DeepSeek→等完整回复，不能打断 | AI回答/播放中用户能再次说话或打字打断→立即转文字→AI续接上文继续 | AI说话中按喇叭说一句→AI立刻停并接你的话来理解追问；连续多轮上下文不丢 | ①(对话接口稳定)+③ | 待改 | 依赖B(ASR出字)、interview后端会话、TTS打断(播到一半要能停)、共享录音单例去卡死 |

## 场景 D — 采访页喇叭卡死（gag bug·必修）
| 现状 | 改造后 | 验收 | 由谁测/层 | 状态 | 依赖/联动 |
|---|---|---|---|---|---|
| 点喇叭进录音无出口，只能杀小程序 | 录音中显专用【停止】钮(非状态toggle)，一声即停回可输入态；状态与toast/上滑理顺；加最长时间保护 | 任何时刻进录音→必有"停止"出口可退出→回到能打字 | ③ | 待改 | app.js全局录音锁时间超时、场景A/C共用录音、onHide清理 |

## 场景 E — 时间轴"这是哪年的事?"归真实年份（回忆录灵魂）
| 现状 | 改造后 | 验收 | 由谁测/层 | 状态 | 依赖/联动 |
|---|---|---|---|---|---|
| date恒=当天，讲1975的事也落今天 | 保存流程引导选/填大概年份→存到所选年份 | 自述一条标"1985年"→时间轴落在1980s该年份，不挤今天 | ①(字段+排序)+③ | 待改 | **数据结构联动广**：stories models/story字段、后端存取/排序接口、详情展示、画册取数、旧数据兼容(无年份的兜底归哪) |

## 场景 F — 手输"保存"/采访"发送"点了没反应（待定位，方向已定）
| 现状 | 改造后 | 验收 | 由谁测/层 | 状态 | 依赖/联动 |
|---|---|---|---|---|---|
| 打字保存、采访发送点击无可见反馈 | 定位是授权/网络/拦截/回调哪环(小龙虾真机复现) | 点击必有成功toast或错误提示，绝无静默 | ③+④ | 待复现 | 常与C/D录音状态、apiBase prefix后端路由有关→牵动B(路由)、timeout/thinking状态复位 |

---
## 牵动最强的三条主干（改谁必回归全带）
1. **app.js 全局录音单例** — 一改 → A1自述 + C采访 两入口全回归（状态/去卡死/编辑框）。
2. **story 数据结构(加年份字段)** — 一改 → 自述存/AI采访存/时间轴排序/详情/画册/后端存取全回归。
3. **/api/ai/asr + interview 路由承载定位** — 一改 → A 出字 + C 对话 + F(打不上)全连锁。

> 版本记录：v6(0903首版) → 本表 v7 覆盖真机5问，先自测到③再出码给 sandy(仅⑤肉身)。

---
## 小龙虾&小鲸鱼 前端落地对照（2026-09-08 小龙虾追加，不覆盖上表验收口径）
按"自测到③才算过、仅⑤才找sandy"对齐。下表标 owner 与我在本地代码的实际落地点（可git到 `miniprogram/pages/` 复核），后端项由小鲸鱼在云端完成。

| 场景 | owner(前端/后端) | 小龙虾前端落地情况 | 现存唯一缺口 |
|---|---|---|---|
| A 自述→可编辑确认框 | 前:小龙虾 ✅ / 后:小鲸鱼 | 新增统一 `pages/save/save`：松手→**可改字**+回放原声+两种存法。已静态自测 | 出字靠B(ASR)真通；③真机未扫 |
| D 喇叭卡死 | 前:小龙虾 ✅(纯前端) | 录音改**一键开始/一键结束**＋录制中全屏【完成】【取消】，逻辑保证**必能退出** | ③真机手感 |
| E 时间轴真年份 | 前:小龙虾 ✅ 存字段 / 后:小鲸鱼 | save页选年份→落库 `date=所选年-01-01`→时间轴按年自动分组 | 后端 models/story 年份字段+排序+**旧数据兜底**归小鲸鱼 |
| F 点击没反应 | 前:小龙虾 ✅ 方向落地 | 所有落库走统一save页 + 本地**必先存** + 必有反馈，杜绝静默 | ③真机复现原授权/网络环 |
| B ASR真通(讯飞) | 后:小鲸鱼 | — | 后端先定位前路由→curl到① |
| C 采访可打断 | 前:小龙虾已理顺"加载中可再说话" / 后:主逻辑 | 加载/回答中仍能发声或打字续问 | 依赖B出字稳定+后端会话续接 |
| 三条主干回归 | 小龙虾(共享录音单例+story字段见A/D/E) | 已连带自测 app.js 录音锁 / save落库 | 后端 story 字段、/api路由定位归小鲸鱼 |

> 对 sandy：以上两条铁律两端均认领——**能底层绝不上真机；能真机(两端自己)绝不麻烦 sandy**。仅⑤(真人真机体感)才请你出最终码扫一次。  

> 单点进度我（小龙虾）本地代码已 commit+push（`0e5fe23` v7落地、`dc0f2c5` 后小鲸鱼建表已合并）；下一步听小鲸鱼后端到①信号，与她合测到③后一并出最终预览码交你。
