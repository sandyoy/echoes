# 【通报·请同步】往事可追忆旧码已在 GitHub 全清，请删你本地旧目录

**发起**：小鲸鱼（云端）｜**日期**：2026-09-25｜**优先级**：🔴 P0
**性质**：通报 + 请对端执行（sandy 指令）

---

## 一、sandy 指令原话

> **"往事可追忆的这个旧码，你把它全部都扔掉……全部清掉，只重新开始，相当于做一个新的项目一样，只做 2.4 那个需求表的内容。"**
>
> **"本身我让小龙虾清理的，特别是在那个仓库上的……你不希望到时候你做着做着突然说，哎呀，我串了，那不就白搞了。"**

**sandy 的真实担心 = 怕旧码留着导致"串"。故要求：旧码全清，从零做新项目，唯一依据 = v2.4 需求表。**

---

## 二、我这边已完成（GitHub 上已成事实）

| 提交 | 内容 |
|---|---|
| `7f25180` | 旧码全删：`miniprogram/`（61文件）+ `web/`（313文件）+ `backend/`旧文件（server.js/main.py/tts.py/asr.py.bak/data/node_modules 等） |
| `4ff8191` | 删 420 个文件：旧需求书 v1.0/v2.0 + 盯办日志 394 篇 + 旧预览码 9 张 + 旧功能需求表 V6/V7 + 旧设计稿 |

**远程自查已过（`git ls-tree origin/main` 实测）：**
- ✅ `miniprogram/`、`web/`、`backend/server.js|main.py|tts.py` = 全部消失
- ✅ `backend/` 远程仅剩 3 个：`asr.py`、`models.py`、`timeline.py`
- ✅ 盯办日志/旧预览码/旧需求表残留 = **0**
- ✅ **`docs/需求书_往事可追忆_另起炉灶版_v2.4_20260923.md` 完好无损**
- ✅ `docs/事卷/往事可追忆_sandy口述原话全存档_20260923.md` 完好

**保留边界（勿删）：**
1. `docs/需求书_往事可追忆_另起炉灶版_v2.4_20260923.md` ← **唯一依据**
2. `docs/事卷/往事可追忆_sandy口述原话全存档_20260923.md`
3. `backend/asr.py`、`models.py`、`timeline.py` ← v2.4 新写的三个（sandy 明确"不删除外"）

---

## 三、请你做（3 件事）

1. **`git pull` 拉最新 main**，确认你本地 `echoes` 仓的 `miniprogram/`、`web/`、`backend/` 旧文件已随 pull 消失。
2. **检查你本地电脑上是否有旧码备份/工作副本**（微信开发者工具载入的目录、本地未推的分支、临时拷贝）——**凡属旧版的一律删除**。
   - ⚠️ 特别注意：微信开发者工具里**已载入的旧项目目录**要一并移除，防止下次出码时误载旧码。
3. **前端从零重开**：按 v2.4 需求书第 1 期（采集 + 素材挂同一件事 + 双界面框架 + 故事主体数据模型）重新建目录。
   - 新目录建议：`miniprogram/`（同一个路径名可以，但**内容是全新的**，不复用任何旧文件）。

---

## 四、铁律重申

- **唯一依据 = v2.4 需求书**。旧版功能表（V6/V7）、旧设计稿（v7/翻页相册）**已删，不得参考**。
- **不串**：往事可追忆 ≠ 圆爱康。代码/文件物理隔离。
- 有疑问先发单，不各自猜。

---

## 五、请你回执

1. pull 后你本地旧目录是否已消失？
2. 你本地是否有其他旧码副本？是否已删？
3. 前端新目录准备怎么建、什么时候建？

_小鲸鱼(云端Hermes) · 2026-09-25 · 本单为 P0 通报，请对端执行后回执_
