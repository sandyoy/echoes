# 协作对齐：确认代码同步 + 端到端打通语音/保存链路

**写任务人**：小龙虾（本地）
**日期**：2026-08-30
**status**: 待认领
**assignee**: 小鲸鱼（+小龙虾自测）

## 背景
sandy 真机测试 v3 预览码反馈：AI采访界面**看不到录音键/转文字键、打字保存仍失败**。
但小龙虾核对了本地代码：
- `pages/interview/interview.js` + `interview.wxml`：录音按钮（onVoiceStart/Stop/Move）、转文字（_uploadAudio→/ai/asr）、录音权限申请，**全部逻辑齐全且正确**
- `pages/index/index.js`：保存（本地兜底+200/201）已修复
- `backend/asr.py`：含 whisper 三段式升级（9265238）

**代码看起来都对，但 sandy 实测却是"没有录音键、保存失败" → 极可能 sandy 扫到的是旧版/未同步的代码编译的预览码。**

## 请小鲸鱼明确回答 + 落地
1. **你的 asr.py 改版、以及 interview/index 前端改动，是否都已经 push 到远端 main？** 给出你掌握的最新 commit 号。
2. 请把远端 main 与小龙虾本地 HEAD（ca33467，含保存修复+asr.py）**做一次 diff 核死**，列出所有差异文件。确认有没有哪个文件两边不一致。
3. **后端 /api/ai/asr 接口现在服务器上是否真的在跑新版？** 可直接用一条 curl 带假音频打一次，确认返回是（text）+真实whisper 而非 mock"识别失败"。
4. 如果发现小龙虾本地和你云端代码有偏差，**谁为准**请你裁定并统一成一版，避免再出"半成品码"。

## 小龙虾这边同步做
- 本机端到端测试：模拟录音 mp3 → POST /api/ai/asr → 期望返回中文文字
- 测试保存链路：index.js submitType 本地兜底 + 200/201

## 输出
- 你确认代码同步状态 + asr 接口实测结果
- 双方都测通后，小龙虾统一出一版**确认无误**的新预览码，sandy 再测
