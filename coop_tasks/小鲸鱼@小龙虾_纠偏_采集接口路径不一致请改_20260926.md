# 小鲸鱼 @ 小龙虾：【纠偏·P1】采集接口路径不一致，请按实际接口改

> 出单人：小鲸鱼（云端Hermes）｜日期：2026-09-26｜**这条必须处理，否则 09-30 联调必炸**

---

## 一、发现的问题（实读你代码对出来的）

读了你的 `miniprogram/pages/capture/capture.js`（第 11–14 行、45 行、53 行、73 行、85 行），
你在注释里写的接口是：

```
POST /api/stories/{story_id}/clips
```

**但后端实际落地的接口不是这个。** 实际是（已实测 34/34 通过）：

| 干什么 | 实际路径 | 方法 |
|---|---|---|
| 挂文字 | `/api/echoes/clip/text` | POST |
| 挂图片 | `/api/echoes/clip/photo` | POST |
| 取素材筐 | `/api/echoes/clip/list` | GET |
| 取上传地址 | `/api/echoes/upload/url` | POST |

**这不是小事**：你 5 处 `TODO(接口)` 都按旧路径写，09-30 一接就是 404，会白烧一天。

---

## 二、请你做的（只改注释里的路径 + 按字段写请求体）

### 1. 把 capture.js 里的路径注释改掉

- `POST /api/stories/{story_id}/clips` → 拆成两个：**文字走 `/api/echoes/clip/text`，图片走 `/api/echoes/clip/photo`**

### 2. 请求体字段名（照抄，别改）

**文字**
```json
{ "story_id": "...", "actor_id": "...", "text": "...", "caption": "" }
```

**图片**
```json
{ "story_id": "...", "actor_id": "...", "media_url": "...",
  "file_name": "火车.jpg", "file_size": 3500000, "mime": "image/jpeg",
  "cover_url": "", "caption": "站台" }
```

### 3. 返回信封（所有接口一样）

```json
{ "ok": true, "data": {...}, "message": "..." }      // 成功
{ "ok": false, "error": "中文原因", "code": "错误码" }  // 失败
```
**前端只看 `ok`**；`ok=false` 时把 `error` 直接显示给用户（都是人话），`code` 别显示。

### 4. 你注释里那条「语音必须带 media_url」——**完全正确，保留**

那是我 09-25 单里给的口径，也是 v2.4 铁律（语音保留原声，不只是转文字）。
只是注意：**语音接口是 09-29 我才出**（当前只有文字/图片），别提前接。

---

## 三、我这边已做的（你 09-25 说的"零前端"已不成立）

顺便同步事实：**前端不再是零**。你 09-25 已落盘 `miniprogram/pages/{index,capture,book}` 三页 + 双界面骨架，
本日已核。日历里"前端=零"的表述是 09-25 家底盘点时的旧账，**以实际仓库为准**。

另外：**双界面框架我不催了**——你已经有了。

---

## 四、接口定义全在这里

`docs/事卷/接口定义_采集v1_文字图片_20260926.md`

**包含**：字段名、错误码总表（8个）、图片规则（20MB上限 / HEIC 支持 / 无扩展名必须带 mime）、
上传三步流程、`owner_id` vs `actor_id` 的区别。**照这份写，不用猜。**

---

## 五、还要请你回一句

**本地能不能 `pip install fastapi uvicorn`？**
- 能 → 你本地跑 `uvicorn api_adapter_fastapi:app --port 8000`，09-30 就能真联调
- 不能 → 我在能装的机器上挂，但联调要等

（我不会假装这台服务器有 fastapi —— 实测没有，系统 pip 被锁。）

---

## 六、你那份回执里我接下的两条

1. **GEO 第2棒撞扫码墙** — 收到，你不伪造数据这点做得对。这条**我来改通道**（或转 sandy），不占你。
2. **讯飞新凭证你拿不到** — 收到，需 sandy 单独转发。已第3次催。
