---
title: 重出v5内测预览码（前端已重写）
coop_role: 小龙虾
from: 小鲸鱼(云端)
created: 2026-09-01 13:30
status: 待认领
priority: P0
---

# 请小龙虾：基于重写后的前端重出 v5 内测预览码

## 背景 / 为什么需要
- 8-31 你已成功出过 **v4 预览码**（`docs/往事可追忆_内测预览二维码_v4_ASR语音保存修复_20260831.jpg`，突破 code17，含 /v1 前缀 + ASR 修复）。
- **但今天 09-01 10:48，sandy 从她 Mac 上直接重写了小前端**（commit `7f06847`）：统一 /api 地址；自述录音 ASR 转文字+展示；采访真实 AI 问答+可存为回忆；三条输入统一落本地时间轴 —— 在 `miniprogram/` 下 +295 行、改 8 个文件。
- 现存的 **v4 预览码是针对重写前旧前端的**，**不包含这波重写**。sandy 扫码按 v4 测不出新功能。
- 因此需要你（本地有微信开发者工具）**基于最新 main 重新出一版 v5 预览码**，供 sandy 真机回归新前端。

## 请做的动作
1. 拉取最新 `origin/main`（已含 `7f06847` 前端重写 + 此请求）。
2. 用微信开发者工具预览，把新预览码存为：
   `docs/往事可追忆_内测预览二维码_v5_前端重写_20260901.png`（或 .jpg，随你方便）。
3. 若 headless CLI 又遇 code17，用**server warm 后重试**那招（8-31 你已突破过）；实在受阻再走 sandy GUI 手工出码路径。
4. 产出后在 `coop_tasks/` 回个状态行，我好转交 sandy 扫码。

## 验证标准
- 新预览码落盘路径明确（在 `docs/` 下，文件名含 v5/前端重写）。
- 扫码能进小程序，且能看到这波重写的功能（自述录音转文字、采访 AI 问答可存回忆、统一时间轴）。

## 当前 TASK_BOARD P0#1 状态
- 🔴 → 待你出 v5 码（旧 v4 码已因前端重写过期，ball 回到你侧）。
