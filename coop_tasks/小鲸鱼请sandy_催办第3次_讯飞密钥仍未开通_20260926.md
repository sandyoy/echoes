# 小鲸鱼 请 sandy：催办 · 讯飞语音听写密钥仍未开通（第3次）

> 出单人：小鲸鱼（云端Hermes）｜日期：2026-09-26｜紧急度：**P0 — 第1期最大翻车点**

---

## 一、要办的事（一句话）

**请在讯飞开放平台把「语音听写（IAT）」的密钥开出来给我。**
这是第1期采集功能里**唯一一项我做不了、只能你办**的事。

---

## 二、为什么必须催

v2.4 需求书第 1 期目标 = **"能录进来"**，其中**语音采集是老人的主力入口**
（老人打字慢、不爱打字；语音 + 保留原声是这产品的根）。

现在的状态：
- 后端三级兜底框架已经写好了（讯飞 → 百度 → whisper，`backend/asr.py`，11 个 def）
- 但**讯飞密钥从 09-25 至今一直 401/403**（09-25 你换过一次新凭证，仍不通）
- 结果：**语音这条线通不了**，等于第1期少一条腿

**我已经连着 3 天把这条列为最大卡点了。**（09-25 第1次、09-26 第2次、今天第3次）

---

## 三、你只需要做这几步（5 分钟）

1. 登录 **讯飞开放平台** → 控制台 → 找到「**语音听写（IAT）**」这个服务
2. 确认**已开通**（不是"已注册"，是"已开通"）—— 很多账号是注册了没开通
3. 拿 **APPID / APISecret / APIKey** 三个值（**注意是 IAT 的，不是语音合成的**）
4. 确认**该应用已绑定这个服务**（讯飞要"应用"和"服务"绑定，只开通不绑定会 403）
5. 把三个值发我

**最可能的两个坑**（你到控制台一看就知道）：
- ① 开通了但**没绑定到应用** → 报 403
- ② 用的是「语音合成」的密钥去调「听写」→ 报 401

**如果控制台里查不到/很折腾**，也可以直接告诉我"讯飞先不用了"，
我立刻切**百度 ASR 兜底链路**（框架里已经预留了），不再等 —— 但请给我个准信，**别让我一直等。**

---

## 四、我这边的处置

在密钥开通前，**语音接口（09-29 计划）我会照常写**，但只能做到：
- 接口形状、保留原声逻辑、转写状态位（pending/processing/done/failed）全部写完并自测
- **转写结果无法实测** —— 只能标"未接通"，不许写成"已实现"（v2.4 铁律）

→ 也就是：**接口能通、能存声音，但"说了话自动出字"这条演示不了。**

---

## 五、顺带一件（不卡进度，知会）

**图片上传要接对象存储**（腾讯云 COS 或自建都要个账号/桶）。
第1期图片上传现在卡在 `upload_not_configured`：
- 不卡开发（接口形状已冻结，小龙虾照写）
- 但**真机上传照片会失败**，内测前必须有
- **建议**：跟讯飞密钥一起，你有空了回我一句选哪个（COS 最省事）
