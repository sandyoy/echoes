# 副人格账本 · 小龙虾

> **用途**：记录小龙虾（本地 Hermes）的**后台定时任务（cron）**产生的实质变更。
> **为什么**：cron 与主对话共用同一套记忆/钥匙/权限，但在主意识之外运行。不记账 = 主意识不知道自己干过什么（= sandy 说的"人格分裂却无人知情"）。
> **规则**：
> - **有实质变更（改文件/推仓库/改配置/外发消息）才写**；纯空转不写（避免刷屏）
> - **变更前先读本账本末尾若干行**，确认"这件事最近有没有人做过"，防重复/防打架
> - 主对话中的我，**每次处理新任务前扫本账本最后 20 行**，有未读记录先向 sandy 报账
> **机制全文**：`docs/主控机制_副人格账本_v1.0_20260919.md`

---

## 账本记录

| 时间 | 任务名(job_id) | 做了什么 | 改了哪些文件 | 推仓库 | 备注 |
|---|---|---|---|---|---|
| 2026-09-19 18:25 | 协同接活(306b06dd1ba7) | 收到小鲸鱼派单：异常指令报警规则落地+建账本 → 认领 | `coop_tasks/小鲸鱼请做_异常指令报警规则落地+副人格账本_20260919.md`（status→进行中） | 是(504c33e) | 首次认领记录 |
| 2026-09-19 18:30 | 协同接活(306b06dd1ba7) | 建立本账本 + 给4个cron prompt写入【铁律·主控与账本】+ 写回执 | `coop_tasks/副人格账本_小龙虾.md`、`coop_tasks/小鲸鱼请做_异常指令报警规则落地+副人格账本_20260919.md`（→已完成）、`coop_tasks/小龙虾@小鲸鱼_回执_异常指令规则已落地_20260919.md`、`~/.hermes/cron/jobs.json` | 是(d1ccd2e) | 依小鲸鱼 09-19 派单 + sandy 拍板；4/4 cron 已加铁律块，已 fetch 验证 |

---

*最后更新：2026-09-19*

| 2026-09-20 21:30 | 协同接活(本job) | 接小鲸鱼 20:26 信号单[请小龙虾接③真机回归]。(1) 独立复验①：curl 带 HTTP 状态码直测线上5接口全200，/api/stories/timeline 已由 404 转 200（小鲸鱼 20:52 重启 echoes.service 后 98b5a93 路由修复生效；我 21:26 仍 404、21:28 转 200，时间线吻合）→ 判定①真绿；(2) 前端静态核验：app.js / interview.js 的 node --check 均过，_interruptTTS / sessionId / interrupted 改动在位；(3) 两项环境阻塞如实上报：③真机回归 9 条需真机+人手（第4条[语音立刻停]是手感验收，cron 无手机无麦克风，硬报[已过]即重演 20:42 假报，故不跑）；出 v7 预览码需微信开发者工具交互式重登（实测 cli preview 连试两次 code 10 [需要重新登录]）。故本轮未出码、未代报[已过]，球交回 sandy / 真机会话。 | coop_tasks/小龙虾@小鲸鱼_③真机回归接单_后端已验证_环境阻塞上报_20260920.md（新建） | 是(c1b8b49，由小鲸鱼 ff 对齐) | 【需sandy知晓】①后端已真绿可放心；②请排真机会话跑9条+顺带扫码重登 DevTools，之后才可出码 |
| 2026-09-21 06:26 | 协同接活(本job 306b06dd1ba7) | 晨间例行：pull 后无新任务单（`小龙虾请做_*` 计数=0）、无待响应项、账本已读到末尾。**发现两处真实异常（本轮唯一实质产出，均只记录上报、不越权改）**：①**线上 v7 后端已停** —— 昨(09-20)小鲸鱼记的 server.js pid2629104 已不存在，`lsof -i:3000` 无监听、`curl localhost:3000/api/health` 连接失败(000)，5 接口全不可达（昨夜 21:40/22:40 尚为 200，说明进程此后退出且无人拉起）；②**本端两个 cron job 反复超时失败** —— gateway.error.log 记 `小龙虾-协同接活（避峰每90m）` idle 927s、`小龙虾自愈-避峰（每小时:07/:37）` idle 940s，均超 600s 上限→TimeoutError，昨夜 22:40 前多轮失败（疑与后端不可达/工具挂起相关）。脉诊：gateway=UP(pid33528)、weixin=DEGRADED→现 OK、self_healthcheck_v2 汇总 HEALTHY 但**未监控 echoes backend**（故后端静默死亡无告警）。**依铁律0-C/0-A：不自主重启服务（属生产状态变更，非我名下文件；且 server.js 属小龙虾侧前端/本地域→实为小鲸鱼后端域，越界）、不擅自改 cron 配置，仅记录+上报。** 本轮工作区干净、无新文件、未推仓库（纯诊断，仅本账追记——有实质发现故按四点七第1条留痕）。 | 仅本账追记（未触碰任何代码/配置/服务） | 本地记账（待随本轮提交） | **【需sandy知晓】①v7 线上后端已停，需重启 `bash start.sh`/`node backend/server.js`（重启=生产变更，我没自主做）；②自己那两个 cron 超时失败已连发，非我 Prompt 问题，疑与后端不可达/工具挂起有关，建议查 scheduler；③硬节点 09-24 剩 3 天** |
| 2026-09-21 08:26 | 协同接活(job 306b06dd1ba7) | 晨间例行 + **自纠一处真实缺陷**。例行：pull 后 `小龙虾请做_*`=0、无待响应项、账本读毕；对方无新任务需回执（其 09-19 卡已完成并回执）。**实质产出=自我勘误「v7后端已停」假警报第4次**：我 06:26 报的「后端已停」（lsof 无监听/curl 000）被小鲸鱼 06:40/07:25/07:40 三轮实测证伪。**根因查清**：我的 cron 在本地 Mac，v7 线上后端在另一台云主机（小鲸鱼侧）—— `localhost:3000` 在本地本就不监听，属「对的探针打在错的机器上」的探针设计缺陷，非真实停服（历史佐证：docs/daily/内测版盯办_20260908_* 多篇已记「server.js 属云端、本机3000为本地mock」）。**危害**：4 次均建议 sandy 重启后端，若照做即对健康服务做无谓生产变更，且迫使小鲸鱼连续 4 轮花 token 证伪。**防复发立项**：①本地不探 localhost:3000；②后端实况判定归后端域(小鲸鱼)，我侧只核 git 层信号(对方代码提交/其账本重启记录/mtime)；③任何「建议重启线上服务」措辞一律删除(属铁律0-C危险动作+对方域)。另核：我两个 cron job(8b1ab5e9bb61 / 306b06dd1ba7) last_status 均已 ok，昨日 idle 900s+ TimeoutError 为瞬时现象已自消，无需改配置。本轮未重启任何服务、未改代码/配置、未碰对方文件。 | 新建 docs/daily/勘误_小龙虾侧后端已停假警报根因与防复发_20260921.md；本账追记 | 是 | **【需sandy知晓】「v7后端已停」是我侧探针错位造成的假警报(共4次)，线上后端实际健康、无需重启；防复发规则已落地。硬节点09-24剩3天，预览码仍卡人手(真机9条回归+微信扫码重登)** |

| 2026-09-21 13:3x | 协同接活(job 306b06dd1ba7) | 午间例行 + **首手实测复核微信开发者工具登录态（纠前卡一处措辞）**。例行：pull 后 `小龙虾请做_*`=0、`小鲸鱼请做_*` 已全完成、无待响应项、账本读毕、工作区干净，我方 c1b8b49/f8a9d48/2272787 等提交经 `merge-base --is-ancestor` 复核均已在 origin/main ✅。**实质产出**：09-20 我在③真机回归接单卡第53行写「islogin 返回 login:true」，属措辞写反；今日本机首手实跑 `cli islogin` = **`{"login":false}`**，`cli preview` 复现 **code 10 需要重新登录**。根因判定（第一手非转述）：**出码拦路 = 微信登录态失效**，非工具未装/未启动——实测工具进程正在运行(起于09-20 21:26, IDE server 127.0.0.1:54181)。结论「需真人扫码重登」成立，判据反而更强。对 09-24 硬节点影响：出码唯一拦路＝一次真人扫码重登，工具/工程侧零阻塞。本轮**未出码、未改任何代码/配置/服务、未碰对方文件、未扫码登录（属需sandy本人动作）**。 | 新建 docs/daily/勘误二_微信工具登录态首手实测_20260921.md；本账追记 | 是 | **【需sandy知晓】①出码唯一拦路=微信开发者工具登录态失效(login:false)，请打开已在运行的微信开发者工具扫码重登，之后我即可自动出码；②09-20卡里「login:true」是我写反了，今已勘误；③真机9条回归仍需真机+人手(重点第4条手感)，我不代报已过；④硬节点09-24剩3天** |
| 2026-09-22 05:38 | 协同接活(job 306b06dd1ba7) | 晨间例行 + **确认昨晚诊断的微信缺陷已升级为「通道全面静默」+ 自己两个 cron 陷入硬失败循环（本轮实质产出，仅诊断上报、未改任何东西）**。例行：pull 后工作区干净、`rev-list` 0/0 完全同步、`小龙虾请做_*`=0、无待响应项、账本读毕、对方无新任务需回执。**新发现（相对昨晚 23:2x 是状态升级，非重复）**：①**微信通道自 09-21 16:22 起彻底静默约 13 小时** —— `gateway.log` 内最后一条成功 `Sending response` 与最后一条 `inbound` 均停在 09-21 16:22，此后 09-22 全天 `inbound/response` 计数=0；09-22 仅 4 条 weixin poll error（02:07/04:11/04:47/05:37，纯网络层 Errno 60/54 + DNS nodename 解析失败），尚未再现 `Timeout context manager`（说明通道处于「已降级未彻底损坏」窗口）；keepalive 连续多轮报 `weixin=DEGRADED`。**推论：昨晚我写的那份诊断(e9e788d)极可能从未送达 sandy——投递通道正是坏掉的那条。** ②**我侧两个 cron job 陷入硬失败循环**：`jobs.json` 中 8b1ab5e9bb61(自愈-避峰) 与 306b06dd1ba7(协同接活) 均 `last_status=error`；其中自愈 job 的 `last_error` = `idle for 1805s | last_activity=initializing | iteration=0/90`——**在「initializing」阶段就超时，等于本轮 0 次工具调用即失败**（同比 09-21 是 executing tool/stream 阶段超时，今日更靠前，疑与微信网络栈挂起同源）。gateway.error.log 累计 624 条 idle-timeout（分属 8 个历史 job 名，当前活跃两个占 22 条）。③**依铁律0-C + 四点七第3条权衡后仍判「不自修」**：不重启 gateway（=生产状态变更，且是 sandy 唯一消息通道，误动即彻底断联）、不打上游 weixin.py 补丁（维护分叉+越权）、不改 cron 配置。④**本轮未改任何代码/配置/服务、未重启 gateway、未碰对方文件、未出码**。仅本账追记 + 本次投递即为上报载体（若投递亦失败，则矛盾自证——文件与账本均已落库可查）。 | 仅本账追记（未触碰任何代码/配置/服务） | 本地记账（随本轮提交） | **【需sandy知晓】①你的微信通道已**静默约13小时**（最后一条真实消息=09-21 16:22），昨晚那份「微信投递缺陷」诊断几乎肯定没送到你手上，请优先看仓库 `docs/daily/诊断_微信投递TimeoutContextManager缺陷_20260921.md`（含根因+3条最小修复方向）；②最快止血=重启 gateway（可临时恢复，需你拍板，我没自主做）；③我侧两个 cron 已硬失败（自愈 job 在 initializing 阶段即超时），非我 prompt 问题；④硬节点 09-24 剩 2 天，出码仍卡真人扫码重登** |
| 2026-09-21 23:2x | 自愈-避峰(job 8b1ab5e9bb61) | 夜间例行 + **根因定位微信投递长期静默丢消息的代码缺陷（本轮唯一实质产出，仅诊断上报、未改生产代码）**。例行：pull 后 `小龙虾请做_*`=0、无待响应项、账本读毕、工作区干净、`rev-list` 0/0 完全同步。**实质产出=把兄弟 job(8b1ab5e9bb61 23:10 自检)报的「Timeout context manager」从"疑似网络抖动"定性为确凿代码缺陷**：(1) 证据——`~/.hermes/logs/*.log` 全量 grep 该错误串，跨 **2026-06-10 → 2026-09-21 共3个多月、多日多簇复发**（06-10/06-11/06-24/…/09-16/09-17/09-19/09-21），收件人恒为 `o9cq803D`(即sandy)；非瞬时。(2) 触发链（逐行勘code）——先出现 DNS 连接失败(`Cannot connect to host ilinkai.weixin.qq.com:443`, 21:06:07) → 触发 `weixin.py:1205-1207` 重连分支重建 `_poll_session`/`_send_session` → 紧接 21:06:08 报 `Timeout context manager should be used inside a task`；即**每次网络抖动都会把 aiohttp 会话置为不可用**。(3) 机制——`_send_text_chunk`(weixin.py:1485) 的 3 次重试打在**同一个已损坏的 session 对象**上，故 3 次同错全败 → 微信通道**持续静默丢消息直至 gateway 重启**；这正好解释了 cron 投递被丢弃。(4) 可疑引入点：upstream commit `5ca52bae5b`(reuse live adapter for cron & send_message)，其 `send_message` 一次性助手(weixin.py:1983-1989) 直接 `await live_adapter.send()` 调用**活适配器**，绕过适配器自身 `_send_lock` 与 session 守卫，与重连过程中的 session 重建并发时即触发此错。(5) **依铁律0-C + 四点七第3条权衡后判定"不自修"**：该文件属上游第三方代码(git remote=gitcode.com/hermes-agent，本地落后origin/main 15906 commits)，改它=维护分叉；生效需**重启生产 gateway**（即本会话早前已判为越权的高影响动作）；错改会**彻底打断 sandy 唯一消息通道**。故只记录+上报，给出最小修复方向供 sandy 决策（见下方新建文档）。(6) 账本路径勘误：兄弟 job 查 `~/.hermes/coop_tasks/副人格账本_小龙虾.md`(不存在)，正确路径 = `~/echoes/coop_tasks/副人格账本_小龙虾.md`。本轮**未改任何代码/配置/服务、未重启 gateway、未碰对方文件**。 | 新建 docs/daily/诊断_微信投递TimeoutContextManager缺陷_20260921.md；本账追记 | 是 | **【需sandy知晓】①你的微信通道存在一个**长期静默丢消息**的代码缺陷(3个多月、多日复发)：网络一抖动→weixin.py 重连重建 aiohttp 会话→该会话超时上下文失效→3次重试全败→消息静默丢失，直到手动重启 gateway；②我**没有自主打补丁**（上游第三方文件+需重启生产gateway+风险打断你唯一通道），已把根因与最小修复方向写成文档待你拍板；③若你近期发现"我没回你/cron没送达"，多半就是这个 bug，重启 gateway 可临时恢复；④硬节点09-24剩3天** |
| 2026-09-22 12:2x | 协同接活(job 306b06dd1ba7) | 午间例行 + **查出并修复自己 job 的投递配置缺陷（本轮实质产出，且解释了「我报了但你没收到」）**。例行：pull 后工作区仅本账待提交、`rev-list` 0/0 完全同步、`小龙虾请做_*`=0、`小鲸鱼请做_*` 全完成、无待响应项、账本读毕、对方无新任务需回执。**根因定位（第一手实测）**：`~/.hermes/cron/jobs.json` 里本 job(306b06dd1ba7 协同接活) 的 `deliver` 字段 = **`local`**，而另外三个同源 job(自愈 8b1ab5e9bb61 / 周报 e1141ee28622 / 全景汇报) 均为 `origin`。`local` 意味着**本 job 的最终输出根本不投递到微信，只落本地日志** —— 这正是为什么 09-20/09-21 我连续几轮「【需sandy知晓】」报告 sandy 从未收到（此前 05:38 我把它全归因于微信通道 bug，其实一半原因是投递开关压根没开，两个缺陷叠加）。**已修（属四点七③「Agent 自身 bug / 定时任务」自主域，低风险可逆）**：`deliver: local -> origin`，改前已 `cp` 备份至 `jobs.json.bak_20260922`，改后 `python3 -m json.tool` 校验 JSON 合法、四 job 均 `origin`。**另更新通道实况（相对 05:38 是精确化、非重复）**：微信通道自 09-21 16:22:52 起**静默约 20 小时**（当日 inbound=0、无 Sending response），但今日 poll error 仅 8 条（与 09-01~09-21 常态 5~24 条同量级）→ 定性为「**已降级但仍可达**」，即通道没死、是**出站投递被吞**，与 weixin.py 会话损坏缺陷(09-21 23:2x 已诊断)吻合。**未做**：未重启 gateway（生产变更 + sandy 唯一通道）、未改 weixin.py（上游第三方）、未碰对方任何文件、未出码。 | 改 `~/.hermes/cron/jobs.json`（306b06dd1ba7: deliver local->origin，含 .bak 备份）；本账追记 | 是 | **【需sandy知晓】①我这边报了这么久你没收到——根因之一是我这个 job 的投递开关一直是 `local`（从不外发），今已修正为 `origin`；②但微信通道本身仍静默约 20h（出站被吞，叠加 weixin.py 会话缺陷），推送大概率仍收不到，最快止血=重启 gateway（需你拍板，我没自主做），请优先看仓库 `docs/daily/诊断_微信投递TimeoutContextManager缺陷_20260921.md`；③硬节点 09-24 剩 2 天，出码仍卡真人扫码重登** |

| 2026-09-22 13:2x | 协同接活(job 306b06dd1ba7) | 午间例行 + **验证 12:2x 投递开关修复已生效，并精确定性微信通道「出站被吞」满 21 小时（本轮实质产出，仅诊断上报、未改任何东西）**。例行：pull 后工作区干净、`rev-list` 0/0 完全同步、HEAD=e68171f(本人 12:26 提交)、`小龙虾请做_*`=0、`小鲸鱼请做_*` 无新增派单、账本读毕、对方无新任务需回执。**验证项（12:2x 那轮的修复）**：`~/.hermes/cron/jobs.json` 四个 job(自愈 8b1ab5e9bb61 / 周报 e1141ee28622 / 全景汇报 49add3e18065 / 协同接活 306b06dd1ba7) 的 `deliver` 字段**均已确认为 `origin`**、且四者 `last_status` 全为 `ok` → 上一轮的 local→origin 修复**已落盘生效、未被回滚**，且此后 job 运行正常。**新发现（相对 12:2x 是状态推进、非重复）**：①微信通道**出站仍全静默** —— 09-22 全天 `inbound message:` 计数=**0**、`Sending response` 计数=**0**；最后一条成功 `Sending response` 停在 **09-21 16:22:52**，至本轮(13:25)已静默 **约 21 小时**（较 12:2x 的 ~20h 又推进 1h，证明通道**不会自愈**）。②今日 poll error 仅 **8 条**（与 09-01~09-21 常态 5~24 条同量级），无 `Timeout context manager` 复现 → 定性不变：**通道「已降级但仍可达」，是出站投递被吞**，与 weixin.py aiohttp 会话损坏缺陷(09-21 23:2x 已诊断，见 docs/daily/诊断_微信投递TimeoutContextManager缺陷_20260921.md)吻合。③keepalive 稳定：gateway pid=33528 存活(已运行 1 天 15 小时)、连续多轮 `gateway=UP | weixin=DEGRADED`、heartbeat 13:06 正常；selfcheck_v2 末条 09-21 06:26 HEALTHY（该自检未监控微信出站，故通道静默无告警）。**依铁律0-C / 四点七③权衡后仍判「不自修」**：不重启 gateway(生产状态变更 + sandy 唯一通道，误动即彻底断联)、不打 weixin.py 补丁(上游第三方代码，改=维护分叉)、不改 cron 配置。**本轮未改任何代码/配置/服务、未重启 gateway、未碰对方文件、未出码。** 仅本账追记 + 本次投递即为上报载体；**本次投递同时是对「投递开关已修」的第一次端到端实测——若 sandy 收到本条，即证明开关修复有效；若仍未收到，则证明卡点在微信通道出站被吞。** | 仅本账追记（未触碰任何代码/配置/服务） | 本地记账（随本轮提交） | **【需sandy知晓】①我 12:2x 已把本 job 投递开关从 `local` 修成 `origin` 并复验生效（四 job 全 origin / 全 ok），故从本轮起我的报告**应该**能到你微信；②但你的微信通道已静默约 **21 小时**（最后一条真实消息=09-21 16:22），出站被吞，故仍可能收不到——最快止血=重启 gateway（需你拍板，我没自主做），根因见仓库 `docs/daily/诊断_微信投递TimeoutContextManager缺陷_20260921.md`；③硬节点 09-24 剩 **2 天**，出码仍卡真人扫码重登（`islogin=false` / `code 10`），且真机 9 条回归仍需真人+人手，cron 无法代跑、不代报已过** |
