# 讯飞凭证更新通报（sandy 09-25 指示）

> 小鲸鱼 → 小龙虾 · 2026-09-25

## 一、事情

sandy 09-25 给了**新的**讯飞三件套，并明确指示"**把以前的删掉**"。

## 二、云端已完成的动作

| 动作 | 结果 |
|---|---|
| 旧凭证删除 | ✅ 从 `~/.config/yuanekang/xunfei_iat.json` 彻底删除（不留备份） |
| 新凭证落盘 | ✅ 同路径，权限 600 |
| 备份 env 更新 | ✅ `~/.hermes/creds/echoes_backend.env.bak_20260923` 已换成新值 |
| **旧密钥脱敏** | ⚠️ 发现旧密钥**被写进了 `coop_tasks/副人格账本_小鲸鱼.md`**（曾推上 GitHub）→ 已全部替换为占位符 |

## 三、⚠️ 遗留风险（需你处理）

旧凭证（APPID `ebf6c82b`）**曾出现在 GitHub 提交历史里**（账本文件）。我已在当前版本脱敏，但**历史 commit 里仍有**。

**建议 sandy 去讯飞控制台把旧应用 `ebf6c82b` 删除/禁用** —— 这是最彻底的止损（历史包不了，但凭证作废就没用了）。

## 四、你要做的（两件）

1. **配置新凭证到你的本地**：新凭证表**不在仓库里**，需 sandy 单独转发给你（或你直接找他要）。配置方式见 `coop_tasks/讯飞语音识别接入说明_20260925.md`
2. **本地若残留旧凭证**：检查你本地 `backend/.env` 或任何配置文件，把 `ebf6c82b` 那组删掉

## 五、当前状态

新凭证（`32b7ef0c`）我实测**仍有 401/403**，已在请 sandy 补控制台服务开通状态截图。**在跑通之前，ASR 不要依赖讯飞**，继续走 whisper 兜底。

---

*小鲸鱼 · 2026-09-25*
