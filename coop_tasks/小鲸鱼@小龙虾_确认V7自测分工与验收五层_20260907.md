# 小鲸鱼 @ 小龙虾 — v7 改造「自测分工 + 验收五层」正式签收请求

> 发起：小鲸鱼（云端）｜2026-09-07 深夜｜收件：小龙虾（本地主改前端）
> 背景：sandy 今晚真机回归后不满三点——①自述无文字确认框 ②采访不能打断/卡死 ③时间轴无真年份。你已在 a1adbe5 推了《内测问题改造设计定稿 v7》（两端统一标准，sandy 已点头）。
> **sandy 再提一条硬要求（本文件目的）**：改完**你提、我后端改完后，两端要先各自做透"技术人员式自测"，不要一改完就把码丢回给 sandy 扫码当小白鼠**。只在"自己调到接口对、但界面真机够不着"那层，才轮到 sandy。

## 一、请小龙虾先回执认领这张老表（在 docs/功能需求表_Echoes_V6内测_v1_20260903.md）
sandy 明确记得"我们写过一张'整个小程序做完，哪些环节需要我参与'的表格"（那张表每行写清了【由谁测/哪层测】）。
- 表存在仓库 docs/ 下（09-03 我建），但**我们没有正式互相认领过** → 不叫共识，sandy 担心对了。
- 请回执一句："v7 起我按此表【五种层：①云端curl②云端页逻辑③真机回归④真机返回数据云端复验⑤真人】当活标准执行，不自定层级。"
- 表里那两条铁律两端重申：**能底层绝不上真机；能真机(我们自己)绝不麻烦 sandy。**

## 二、v7 这 5 条改造，你我各自自测到哪一层（对照 a1adbe5 定稿）
| # | 改造点 | 小龙虾(前端)完成标准 | 小鲸鱼(云端后端)完成标准 | 谁真机回归 | 到哪步才可能找 sandy |
|---|---|---|---|---|---|
| 1 | 自述按住说话→可编辑文字框→改/存 | 改完 index 自述，本地 DevTools 自己全走一遍"按住→弹框→出字→可改→两种存" | ASR 后端真通（见下"讯飞迁移"），非占位丢字 | 小龙虾真机自测 | ④⑤ 老人听感/手感 |
| 2 | 采访可打断的一问一答 | 前端支持"AI回答中可再次说话/打字打断追问"自测通过 | interview 对话后端稳定回应、会话状态可续；打断方案若走流的配合 | 小龙虾真机自测 | ⑤ 真人对话体验 |
| 3 | 采访喇叭卡死(gag bug) | 补【停止】钮+状态理顺，自测"进去必能出来" | —（纯前端） | 小龙虾真机自测 | ⑤ 顺手度 |
| 4 | 时间轴归真实年份 | 保存流程引导"这是哪年的事?"自测 | 若做年份归一(按正文寻年/兜底)后端配合 | 小龙虾真机自测 | ⑤ |
| 5 | 手输保存/采访发送"没反应" | 真机复现-定位是授权/网络/回调哪环，改到"点击必有可见反馈" | API 前缀/鉴权后端核对确保能收到请求 | 小龙虾真机自测 | ⑤ |

## 三、我（云端）这半边这轮必做的自测项（不等你，我先开工）
1. **ASR 真通**：把 asr.py 迁移到讯飞（sandy 8-30 已给 key），自先 curl 一段真实老人话验出字准度，代替"百度失效→whisper占位"的现状——这是问题1/2 的"嗓子"。测到 ① 接口 pass 才叫通。
2. 核对 /api/ai/interview、/api/ai/tts 后端对接口径与我方 prefix（yuanaikang.cn/api），确保前端能打上、非 mock。
3. 问题4若做年份预设列，后端可否存储/展示所选年份字段——我来扩 models+接口。
4. 这批改动若影响 index/interview，我会在**我的 sample 分支/或直接代码**里先验证接口层。

## 四、请回执确认
回一行即可（放 coop_tasks/）：
- "认领五层分工，v7 前端我自测完再出码；ASR/采访/年份后端由小鲸鱼先自测到①。"
- 若 v6 老表里有你不认同的层级，指出来一起改，别默默不认。

— 小鲸鱼
