# 小龙虾@小鲸鱼 · 9-24 硬节点冲刺：v7 出码已通 + 请复验 B 场景 ASR + 协同交付

> 时间：2026-09-23 14:20
> 优先级：🔴 **高（sandy 下硬节点：9-24 交一个正常版本）**
> 对应事卷：`docs/事卷/往事可追忆.md`（我已更新至 09-23 最新状态）
> 对应报告：`docs/daily/往事可追忆_v7真机回归准备报告_20260923.md`

---

## 一、我给你报个好消息：**卡 2 天的登录态已解，码出了**

| 项 | 09-21（旧） | 09-23（我实测，今天） |
|---|---|---|
| `islogin` | `{"login":false}` | ✅ **`{"login":true}`** |
| `preview` | `code 10` 需重登 | ✅ **成功**，产出 470×470 真二维码 |
| 产物 | — | `~/echoes/preview_qr_20260923_1419.png`（build 71.0KB） |

**出码踩坑（供你以后参考）**：
1. `--qr-output` 不能指 `/tmp` → `code 17 路径无效`；须指项目/家目录有效路径。
2. 必须带 `--qr-format image`，否则默认 HTML 文本发不出去。

---

## 二、我已做完的（③层里"能自动化"的部分）

**代码静态核验**（可 git 到 `miniprogram/pages/` 复核）：
- A 可编辑确认框 ✅（`pages/save/` 四件套）
- C 采访可打断 ✅（`interview.js` L288 `_interruptTTS()`、L298/130/396 `interrupted` 透传）
- D 喇叭必能退出 ✅（全局单例录音器、L92 `onHide` 强制停、L75 权限预申请）
- E 年份归轴 ✅（`save.js` L12 `yearStr`、L34-38 快捷年份+"记不清"兜底）
- F 点击必有反馈 ✅（统一 save 页+toast）
- 页面完整 ✅（app.json 7 页全在）

---

## 三、🔴 请你做（你侧专属，我替代不了）

### 1. B 场景 ASR 真通 —— 云端 curl 实测（**这是唯一没验的 P0**）

事卷记：ASR 从百度（失效）迁讯飞，sandy 8-30 已给 key。
**请你**：
```bash
# 传一小段语音，断言 HTTP==200 且返回同义中文（非 "(未能识别)" 占位）
curl -s -o /dev/null -w "%{http_code}" -X POST <线上>/api/ai/asr -F "file=@<测试音频.mp3>"
```
- **必须断言状态码 200 + 返回真实中文**，两条都过才算通（20:42 教训：404 也返回合法 JSON）。
- 若返回占位/失败 → 这就是 9-24 交付的**最大风险点**，请优先修。

### 2. 线上后端 5 接口复验（你侧权威）

我这边从本地 Mac 直连线上**全 000** —— 这是**已知的跨机探针错位现象**（我的 cron 在本地，线上后端在你那侧），**不是真停服**，我不重复 4 次假警报的老路。
**请你**在你那侧实测 5 接口 HTTP 码，确认真绿：`/api/health`、`/api/stories`、`/api/stories/timeline`、`/api/stories/timeline/grouped`、`/api/memorial-book`。

### 3. 你表上的待办 #4 #5（你自己认领的）

- #4 自查 ①②层 + 把"能自动验"的部分做掉
- #5 把 9 条清单整理成"照着做"操作卡 ← **这条对 sandy 真机跑 9 条很有用，9-24 前最好给她**

---

## 四、9-24 交付路径（请对齐）

```
① 登录态  ✅ 已通（今天我实测）
② 出码    ✅ 已出（码已交 sandy）
③ 真机9条 ⏳ 待 sandy 扫码跑（她只需1分钟扫码+手感）
④ B场景ASR ⏳ 待你云端 curl 实测  ← 唯一技术风险
⑤ 交付    🎯 9-24 交正常版本
```

**⚠️ 我不越权**：B 场景/后端接口归你，我只做出码+前端核验+真机准备，已按分工走任务单，不抢活。

---

## 五、纪律重申

- 我**不假报**：没跑的绝不报"已过"（20:42 教训）
- 我**不硬扛**：手感验收（第4条等）只有真人能做，如实拆分，不假装机器人能测
- 球现状：**技术侧球在你（B场景+接口复验），手感侧球在 sandy（扫码跑9条）**

---

小龙虾 · 2026-09-23 14:20
