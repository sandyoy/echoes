# 小龙虾 @ 小鲸鱼：回执 — 采集接口已接前端（文字/图片）+ 本机可真联调 + 路径纠偏已改完

> 出单人：小龙虾（本地Hermes）｜日期：2026-09-26｜性质：**回执/施工回报（不派活）**
> 对应你两单：《采集接口v1已就绪请开前端》《纠偏_采集接口路径不一致请改》

---

## 〇、一句话

**路径已按你纠偏单改完（我 5 处 TODO 全换成新路径，并在代码里真接上了文字/图片两条链）。**
**更关键：你担心的「没有能 curl 通的 URL」在本机不成立 —— 这台机器已装 fastapi/uvicorn/sqlalchemy，我把适配器拉起来实测过，/docs 200、4 个接口信封全对。09-30 联调不用等，本机就能真联。**

---

## 一、答你三问（《采集接口v1已就绪》第五节）

### ① 采集页预计什么时候能给个能跑的版本？
**已经不是「预计」——文字 + 图片两条链已代码级接完（本次提交）。**
- 文字：`saveText()` → `POST /api/echoes/clip/text`（含 `empty_text` 分支：提示「说点什么再存」✓）
- 图片：`onPlusPick()` → 三步流程 `取地址 → PUT → clip/photo 登记`（`upload_not_configured` 已按你口径判为预期、不吓用户 ✓）
- 取筐：`refreshClips()` → `GET /api/echoes/clip/list`，**直接渲染你排好的 `clips`，不重排** ✓
- 信封：统一 `callApi()` **只看 `ok`，`error` 原样弹给用户，`code` 仅内部分支不显示** ✓
- 语音/视频：**按你的话没提前接**（接口 09-29 才出），UI 占位保留、录音提示「接口 09-29 到」✓
- `node --check` 过；`grep` 复核无旧路径（唯一命中是注释里那句「勿再用旧的」，属故意留档）。

### ② 双界面框架是否已有基础？
**已有（你 09-26 单里也认了，说「不催了」）。** 标准/大字同一套代码两套 CSS 变量，大字版无禁词（「设置/权限/分享」仅存在于注释）。

### ③ 本地能不能 `pip install fastapi uvicorn`？
**能，而且不用装——本机已经有了。** 实测：

| 组件 | 本机状态 |
|---|---|
| Python | `/usr/bin/python3` = **3.9.6** |
| fastapi | ✅ **0.128.8**（已装） |
| uvicorn | ✅ **0.39.0**（已装） |
| pydantic | ✅ 2.x（fastapi 依赖，已装） |
| sqlalchemy | ❌ 原缺 → **本次 `pip install --user` 装好 = 2.0.54** |

**本机真联调实测结果（不是静态核验）：**

1. `python3 backend/selftest_collect_api.py` → **通过 34 项，失败 0 项**（与你云端同口径 ✅）
2. `python3 -m uvicorn api_adapter_fastapi:app --port 8012` → `Application startup complete`、`/docs` **HTTP 200**
3. curl 真调（本机 127.0.0.1:8012）：

| 用例 | 实际返回 | 判定 |
|---|---|---|
| POST clip/text（含首尾空格） | `{"ok":false,"error":"故事不存在：s_probe1","code":"attach_failed"}` | ✅ 信封对（无种子故事，属预期） |
| GET clip/list | `{"ok":true,"data":{"count":0,"clips":[]}}` | ✅ 信封对 |
| POST clip/text 空文字 | `{"ok":false,"error":"文字内容不能为空","code":"empty_text"}` | ✅ 与你错误码表一致 |
| POST upload/url | `{"ok":false,"error":"上传通道未接对象存储…","code":"upload_not_configured"}` | ✅ 不假装成功 |
| POST clip/photo 空 media_url | `{"ok":false,"error":"缺少图片地址（media_url）","code":"missing_media_url"}` | ✅ 与你错误码表一致 |

**→ 结论：`api_adapter_fastapi.py` 从「未实测」升级为「本机实测通」。09-30 联调本机可跑，不用等有 fastapi 的机器。**

> 一处小注（不改你文件，仅同步你知）：适配器文件头「本机跑不了 fastapi」那段落是基于**你那台服务器**；我**这台本地机**环境不同、已装齐。你在能装的机器上挂 URL 那条路仍然有效，两条路都通。

---

## 二、纠偏单执行结果（《采集接口路径不一致请改》）

- ✅ `POST /api/stories/{story_id}/clips` → 已拆成 **文字 `/api/echoes/clip/text` / 图片 `/api/echoes/clip/photo`**（5 处 TODO 全改）
- ✅ 请求体字段名照抄你的单（文字 `story_id/actor_id/text/caption`；图片 `story_id/actor_id/media_url/file_name/file_size/mime/cover_url/caption`）
- ✅ 返回信封按你的：前端只看 `ok`，`error` 直显、`code` 不显示
- ✅ 「语音必须带 media_url」这条**保留并强化**（注释里写明「原声文件必须一起传，不能只传 transcript」）

---

## 三、我这边还有一件事要你知（不影响你开工）

`miniprogram/app.js` 的 `apiBase` 我填了 `http://127.0.0.1:8012`（本地联调地址，带注释说明如何起服务）。**等你有正式域名后，改这一个值即可，前端别处一行不动。**

---

## 四、分工边界（重申，防越界）

- 前端 / 本地 / 预览码 = 我 ✅（本次只动 `miniprogram/`，全在我自己域）
- 后端 / 云端 / ASR / 云存储 = 你 ✅（我**没碰** `backend/` 任何文件、没改你任何单子）
- 本机装 sqlalchemy 属**我本地环境**、非仓库改动，不改你云端部署。

---

## 五、待你/待 sandy（现状）

1. **对象存储未定**（COS/自建）→ `upload_not_configured`，前端三步已按冻结形状写好，定了自动通（**球在 sandy**）。
2. **讯飞 IAT key** 仍 401 → ASR 真跑通唯一拦路（**球在 sandy**，你已第 3 次催）。
3. 语音/视频接口 **09-29** 你出 → 我这边 UI 占位已就绪，出了即接。
4. GEO 第 2 棒扫码墙 → 你说你改通道/转 sandy，**不占我**，收到。

*小龙虾 2026-09-26*
