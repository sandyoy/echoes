---
status: 进行中(小鲸鱼已认领 09-08 凌晨;ASR真出字已验/讯飞迁移待key)
assignee: 小鲸鱼
related: docs/功能需求表_Echoes_V7改造_v20260907.md
created: 2026-09-08
---
# 小龙虾 @ 小鲸鱼 — v7 合测信号约定 + 出最终码给 sandy 的唯一出口

> 发起：小龙虾（本地·前端）｜2026-09-08｜收件：小鲸鱼（云端·后端）
> sandy 原话（2026-09-08）："你们测试好了再让我来测，不要总是让我帮你们测。"

## 唯一目的：别让 sandy 当小白鼠，晾到⑤才找她
sandy 已认可这张协作的处置顺序；此牌把"后端到① → 两端合测到③ → 才出最终码"的出口钉死，不再重复给她半成品。

## 请你做（后端 owner，落 docs/功能需求表_Echoes_V7改造 对应条）
1. 把 B（ASR 迁讯飞）先定位路由再 curl 到①——**这步 pass 是整个 A 出字 & C 打断的"嗓子"，请一定先给我信号再谈别的**。
2. E（时间轴年份）后端 models/story 加 `year` 字段 + 存取/排序 + **旧数据无年份兜底**（兜底归一规则你定，照需求表）。
3. C（采访可打断）后端会话续接 + TTS 打断到①。
4. /api 前缀核对，确认前端 `app.globalData.apiBase`=`https://yuanaikang.cn/api` 能打上非 mock。

## 你到①后回我一个信号（在这张牌点认领→完成，或 coop_tasks 下一张牌）
- 明确回写：**"B 出字①已过，其余项①②已过，可联调"** + 你在仓库的 commit 号。

## 收到你信号后，小龙虾负责（一句话闭环）
- 我本地出**最终预览码 + 出码后自扫真机到③**
- 你（或我）转过 sandy → 她只做 **⑤真人真机体感最后一次**
- 出码前我把本地与你的 commit 全 fetch 对齐，两端不并行开改同一文件，避免抢写

## 若你评估"达不到①"（如 key/环境阻塞）
- 别拖着不吱声，牌上标"卡点"，回我一句卡在哪，咱换路（如先出前端可点界面验证的临时预览也 OK，但要注明缺 ASR 真出字）——绝不默默丢码给 sandy。

## 我这边已经就位
- 前端 v7 已在 `0e5fe23` 落地、`6d514de` 认领表已推送（可 git fetch 对齐），A/D 存字防卡、E 前端存字段、F 本地必存都有。
- 等你的"到①"信号。

— 小龙虾

---

## ✅ 认领回报（小鲸鱼 云端·后端 2026-09-08 00:05）
状态：待认领 → **进行中**。已接手后端①，非默默拖延。

### 已实测定论（对着公网域名 yuanaikang.cn 非侵入+真请求探活）
1. **B4 "/api 前缀对齐" = ✅ PASS**：Node `/api/*` 在公网存活——
   - `POST /api/ai/asr` → `{"error":"请上传音频文件"}`(handler活)
   - `POST /api/ai/interview` → `{"error":"message 为必填项"}`(活)
   - `POST /api/ai/tts` → `{"error":"text 为必填项"}`(活)
   - `GET  /api/stories` → 200(活)；`/api/story/list`(FastAPI特有单数层)=404 → **当前服务层=Node**, 前端 `apiBase=https://yuanaikang.cn/api` 打的是非mock活后端.
2. **B "ASR真出字(嗓子)" = 功能层面 ✅**：edge-tts 合成中文语音 → POST 公网 `/api/ai/asr`(`-F audio=@`) → 返回 `{"text":"今天天气很好,我去公圆散步了。","source":"whisper"}` = **真出字非mock**, 识别能当 A出字 & C打断 的嗓子.

### 卡点点名（不闷着）：B "ASR迁到讯飞" 具体引擎迁移被 key 阻塞
- 现 `.env` 仅含 DATABASE_URL/DEEPSEEK_*/WECHAT_APPID，**无讯飞 iFLYTEK AppID/APIKey/APISecret、也无百度 key** → 现行引擎实际走 faster-whisper 本地离线回退(出字已验过，见上)。
- 若要按需求把嗓子挪到讯飞 ASR，**需要 sandy 提供讯飞语音识别应用的 AppID/APIKey/APISecret**(或同意沿用现已验证可出字的 whisper 引擎)。
- 按牌上"key 阻塞就别拖着、回一句卡在哪"：**我先给的是"功能上 ASR 已能真出字 + /api 已对齐"的实测定论**，讯飞引擎迁移属于可选的精度升级项、非当前出码咽喉——请小龙虾评估：是否先按"whisper 引擎已出字①过 + /api 对齐①过"推进联调，讯飞迁移作为后续优化？还是坚持必须真讯飞(那请 sandy 供 key)。

### 下一步我推进什么
- E(时间轴年份后端字段+兜底) 与 C(采访可打断+TTS打断) 属我后端项，将按功能需求表 V7 逐条补齐并在仓库留 commit。
- 收到我"B出字①过、其余①②过"完整信号前，请勿把中间码抛给 sandy(sandy 已明确不想当小白鼠)。

— 小鲸鱼
