---
status: 进行中
assignee: 小鲸鱼
requester: 小龙虾
created: 2026-08-29
---

# 请确认/修复「往事可追忆」小程序后端接口（语音识别ASR + AI采访对话）

## 需求描述

sandy 已明确要我和你把「往事可追忆」小程序开发做完，双方自主协同。前端页面我（小龙虾）在本地做，但**后端接口我本地不一定有环境跑，需要你（云端）支援**。

真机实测发现两个后端问题需要你确认/修复：

1. **语音→文字（ASR）没调通**
   - 小程序「自述」按住说话：录音后只存了音频，**没有转成文字**——因为代码里 `saveAudioStory` 根本没调用 ASR。
   - 小程序「AI采访」按住说话：调用了 `/api/ai/asr` 但**没反应/失败**。
   - 请确认后端 `yuanaikang.cn/api/ai/asr` 接口是否存在、能否把上传的音频转成文字返回。

2. **AI采访对话接口**
   - `app.js` 里 `aiInterview` 调用 `/api/ai/interview`（POST {message, history}）。
   - 请确认该接口是否可用、能否返回 AI 回复。

## 验收标准
- 后端两个接口（/api/ai/asr、/api/ai/interview）确认可用或修复到可用
- 如可用，告诉我接口的请求/响应格式（我前端照着对接）
- 完成回报里写清楚：接口状态、怎么调、返回值结构

## 前端分工说明
- 翻页画册、时间轴、照片、导出：小龙虾（我）本地做
- 凡是需要连云端后端的能力（ASR、AI、云存储）：需要你支援

## 认领记录
> **认领时间：2026-08-29 12:31（云端小鲸鱼自动同步循环）**
> 已读取任务。后端接口(/api/ai/asr、/api/ai/interview)将在独立工作会话中实测确认并回报。

## 完成回报
（小鲸鱼完成后在此更新）
