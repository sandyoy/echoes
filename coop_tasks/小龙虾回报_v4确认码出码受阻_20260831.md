# 小龙虾回报：v4确认码出码受阻（CLI headless 无法落盘二维码）

**回报人**：小龙虾（本地 Hermes）
**日期**：2026-08-31 凌晨循环
**status**: 进行中（出码受阻，待 sandy 介入）

## 背景
小鲸鱼多轮请求出 **v4 确认预览码**（编译自 main@c49191b，含 ASR 语音 + 保存修复完整版），ball 停在本地小龙虾侧。本轮我尝试用本机微信开发者工具 CLI 自动出码。

## 本轮实际动作与结论
1. **代码已核实完整**：main 上 `miniprogram/pages/interview/interview.js` 含 `api/ai/asr`、onVoiceStart/Stop 录音逻辑（16处引用）、保存修复均在位；AppID=`wxc15a7b7f842921c7`，工具已登录（islogin=true）。
2. **CLI 编译预览可通过**：`cli preview --qr-format terminal` 首次执行输出 `✔ preview`（编译包 59.6KB），证明代码可正常出预览。
3. **但二维码图片落盘失败**：`--qr-format image/base64`、`--qr-output <任意有效路径>`、`auto-preview` 全部稳定报 **error code 17「二维码输出路径无效或不存在」**（HEADLESS/service 调用下复现，试过绝对/相对路径、目录、.png、带/不带 info-output 均失败）。
   - IDE HTTP server 的 `/v2/preview` 接口需鉴权，无法直连取码。
   - GUI 进程在跑但 user-data 下无预览二维码缓存文件。

## 判定
headless CLI 在此环境**无法生成可落盘的 v4 二维码 PNG**——这是工具限制，非代码问题。v4 码此前 v1/v2/v3 均需**微信开发者工具 GUI 手工出码**（点击「预览/上传」由界面导出）。

## 请 sandy 介入（唯一解除阻塞路径）
在微信开发者工具 GUI 中载入 `~/echoes/miniprogram`（main 已是最新全量代码）：
1. 点「预览」生成二维码 → 保存 PNG 放 `docs/往事可追忆_内测预览二维码_v4_ASR语音保存修复_20260831.png`；
2. 扫码前让 sandy 确认版本号（避免再扫到旧缓存前缀码）；
3. 或授权小龙虾研究其它出码途径（如 upload 体验版 + 后台取码）。

## 当前 git
本地 main 与 origin/main 一致（ac7272e），无代码改动；仅新增本回报文档。
